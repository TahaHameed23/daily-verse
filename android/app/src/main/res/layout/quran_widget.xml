<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="8dp"
    android:background="@drawable/widget_background">

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="4dp">

        <TextView
            android:id="@+id/widget_chapter_info"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Loading..."
            android:textSize="10sp"
            android:textColor="#666666"
            android:fontFamily="sans-serif-medium" />

        <ImageButton
            android:id="@+id/widget_refresh_button"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_refresh"
            android:contentDescription="Refresh verse" />

    </LinearLayout>

    <!-- Arabic Text -->
    <TextView
        android:id="@+id/widget_arabic_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text=""
        android:textSize="14sp"
        android:textColor="#333333"
        android:gravity="right"
        android:fontFamily="serif"
        android:lineSpacingExtra="2dp"
        android:paddingBottom="4dp"
        android:visibility="gone" />

    <!-- Translation -->
    <TextView
        android:id="@+id/widget_translation_text"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:text="Loading verse..."
        android:textSize="12sp"
        android:textColor="#333333"
        android:gravity="top"
        android:fontFamily="sans-serif"
        android:lineSpacingExtra="1dp" />

    <!-- Footer -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="4dp">

        <ImageButton
            android:id="@+id/widget_favorite_button"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_favorite"
            android:contentDescription="Add to favorites" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Quran Verses"
            android:textSize="8sp"
            android:textColor="#999999"
            android:gravity="right" />

    </LinearLayout>

</LinearLayout>
